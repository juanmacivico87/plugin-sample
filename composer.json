{
    "name": "plugin/sample",
    "description": "{{ plugin_description }}",
    "require": {
        "php": ">=7.3.0"
    },
    "autoload": {
        "psr-4": {
            "PrefixConfig\\": "config/",
            "PrefixSource\\": "src/"
        }
    }
}